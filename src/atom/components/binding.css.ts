import { css } from "../lib/Atom";

export const styles = css`
    span {
        background-color: var(--atom-control-bg);
        color: var(--atom-fg);
        padding: 4px;
        padding-top: 3px;
        padding-bottom: 4px;
        border-radius: 4px;
        font-size: 80%;
    }
`;
